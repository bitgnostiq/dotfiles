;;; init-pinentry.el --- GPG pinentry in Emacs -*- lexical-binding: t -*-

;;; Code:

(use-package pinentry
  :elpaca t
  :config
  (pinentry-start))

(provide 'init-pinentry)
;;; init-pinentry.el ends here
